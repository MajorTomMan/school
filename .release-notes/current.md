## 修改点

- 设置页顶部改为紧凑横向菜单，按“代理 / 更新 / AI”从左到右排列，不再使用平均分栏式入口。
- 应用更新从代理页面独立出来，拥有单独的“更新”页面。
- 代理页面只保留代理地址、版本更新代理开关、AI 代理开关和保存状态。
- 更新页面单独展示当前版本、更新通道、自动检查、仅 Wi-Fi 下载、手动检查和更新状态。
- AI 页面继续保留接口地址、模型、API Key、连接测试和本地学习记录清理功能。

## 修复点

- 更新与代理的界面职责完全分离，同时保留“版本更新是否走代理”的独立控制。
- 横向菜单按文字宽度排列并使用短下划线标记当前页面，避免三个入口被拉伸成大块分区。
- 学科管理入口继续保持移除，不会重新出现在设置页面。
