## 修改点

- APK 改为纯云端课程运行时，不再打包教材目录、章节标题、课程正文、例题、小结、习题或教材 PDF。
- 结构化课程数据和完整课程 ZIP 由稳定的 GitHub `course-latest` Release 发布，APK 默认读取其中的 `manifest.json`。
- 教材 PDF 暂时保存在 Google Drive，作为课程清单中的外部文件按需下载，不进入 GitHub 仓库、课程 ZIP 或 Release。
- 完整课程 ZIP 解压后会补齐外部教材；旧缓存中摘要一致的 PDF 会直接复用，只有缺失或变化时才重新下载。
- 更新计划会把外部 PDF 的传输体积计入全量成本，继续支持文件级增量更新和完整包回退。
- 教材 PDF 下载后会校验文件大小、SHA-256、PDF 文件头和 202 页页数，再与课程数据一起原子切换到 `active`。
- 教材中心只展示已经从云端下载并验证的教材，不再提供本地选择、绑定或替换 PDF。
- 首次打开且尚未安装课程时，先提示课程包与教材 PDF 的下载体积，只有用户确认后才开始下载。
- 首次下载、完整更新和增量更新都使用确认弹窗；选择“稍后”时本次不下载，下次启动仍会再次检查并提示。
- 课程下载由 WorkManager 前台任务执行，切到后台后仍会继续；应用内和通知栏同时显示文件名、字节数与百分比进度。
- 下载完成或失败后发送独立结果通知，下载成功后立即刷新教材目录。
- 课程清单、GitHub Release 资源和 Google Drive 教材 PDF 全部通过应用现有的“更新下载”代理路由访问。
- APK 版本升级为 `0.21.3`，CI 构建不再在用户可见版本名后追加 `+dev.构建号`。
- 课程页面支持教材起止页范围，跨页知识点会生成正确的教材阅读窗口。
- 课程 Release 发布前会下载原始教材 PDF，并逐页核对课程声明的教材页码和原文锚点。
- 数轴交互改用独立的有符号移动与绝对值数学模型，Compose 只负责绘制经过计算的坐标。
- 跨页课程标题会显示完整教材页码范围，例如“教材第 13—14 页”。
- 有理数分类图改为可点击的双维矩阵，同时呈现“按符号”和“按表示形式”两条分类路径。
- 课程页保留左右翻页，并在单页内容超过屏幕时支持上下滚动，为后续几何、函数、方程和统计课程提供统一长页容器。
- 有理数分类改为具体数字、双分类标签和数轴位置联动，点击数字时会同步高亮分类结果与数轴点。

## 修复点

- 完整课程 ZIP 不再重复携带十余 MB 的教材 PDF，课程内容更新时不会重复发布或下载未变化的教材。
- 新增 `inFullPackage` 文件声明，明确区分 Release 包内资源和 Google Drive 外部资源。
- 完整安装时若 ZIP 缺少外部文件，会从旧缓存安全复用或通过清单地址补齐，不再直接判定课程包损坏。
- GitHub Actions 自动构建、验证并覆盖发布 `manifest.json`、课程 JSON 和不含 PDF 的完整 ZIP。
- 未安装课程时不再在后台静默开始首次下载，避免在用户未确认的情况下消耗网络流量。
- 已安装课程只在后台检查小型清单；检测到增量或完整更新后先征得用户确认，不再静默下载。
- 增量更新失败并回退完整课程包时会动态调整总下载量，通知栏与应用内进度保持一致。
- 教材 PDF 下载不完整时会明确提示检查 Google Drive 的公开共享权限，不再只显示笼统的文件不完整错误。
- 内部滚动发布仍保留 `dev-latest` 地址以兼容旧版 APK，但应用版本名和更新提示只显示正式语义版本。
- 修正第一章课程中“有理数、数轴、相反数、绝对值、大小比较”的印刷页码，并改用已从教材 PDF 核对的原文。
- 1.2 复习题改为教材第 16 页的实际题目，第一章小结和章末复习题分别对应教材第 21 页与第 22—23 页。
- 数轴固定以0为中心，负数位于左侧、正数位于右侧；终点严格按“起点+位移”计算，正位移向右、负位移向左。
- 绝对值图同时标出x与|x|：x小于0时在正半轴显示镜像位置，x大于0时在同一坐标上下分别标注两者。
- 原分类图只把类别并排摆放，无法明确表示交叉关系；新版会高亮具体例子对应的符号类别与表示形式。
- 原课程页必须把正文和固定高度可视化压进一屏；新版允许当前知识页自然延长并滚动阅读。
